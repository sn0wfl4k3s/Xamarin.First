<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:FirstApp.Models"
             x:Class="FirstApp.Views.MainPage"
             Title="NoteApp">
    <StackLayout Orientation="Vertical">

        <Button 
            Text="Criar nova nota" 
            CornerRadius="30" 
            Margin="30,5,30,10" 
            Command="{Binding OpenCreateNotePageCommand}"
            />

        <Frame>
            <Label Text="Últimas Notas:" FontSize="Large" />
        </Frame>


        <ListView 
            ItemsSource="{Binding Notes}"
            HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Note">
                    <ViewCell>
                        <StackLayout Padding="5,0,0,8">
                            <Label Text="{Binding Title}" FontSize="Large" />
                            <Label Text="{Binding Description}" FontSize="Medium" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>
</ContentPage>
